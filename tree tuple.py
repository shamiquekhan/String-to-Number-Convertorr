class TreeNode:
    def __init__(self,key):
        self.key=key
        self.left=None
        self.right=None
    def parse_tuple(node):
        if isinstance(Data,TreeNode) and node.left is not None and node.right is not None:
            node_mid=node.key
            node.left=tree_to_tuple(node.left)
            node.right=tree_to_tuple(node.right)
        elif  is None:
            node.left=None
        else:
            node.right=None
        return node 
tree_tuple=(((1,3,None),2,((None,3,4),5,(6,7,8))))
